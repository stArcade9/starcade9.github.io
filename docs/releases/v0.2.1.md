# Nova64 v0.2.1

**Released:** 2026-03-19

Patch release: formatting and lint-scope hardening on top of v0.2.0.

## Changes

- `fix: run prettier on all files, scope lint to runtime+src only`

Lint is scoped to `runtime/` and `src/` so example carts don't fail CI on style,
and the tree is fully Prettier-formatted.

**Full history:** `git log v0.2.0..v0.2.1`
